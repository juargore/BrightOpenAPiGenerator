pluginManagement {
    repositories {
        google()
        gradlePluginPortal()
        mavenCentral()
    }
}

rootProject.name = "BrightOpenApiGenerator"
include(":androidApp")
include(":shared")